require 'spec_helper'

describe Paperclip::DocsplitPdf do
  describe "converts documents to PDF" do
    it "converts .docx documents to PDF" do
      pending
    end
    
    it "performs no conversion if the document is already a PDF" do
      pending
    end
    
    it "raises an error if PDF conversion was unsuccessful" do
      pending
    end
  end
  
  describe "knows whether or not a document is in PDF format" do
    it "identifies a PDF document as a PDF" do
      pending
    end
  
    it "identifies a non-PDF document as not a PDF" do
      pending
    end
  end
end